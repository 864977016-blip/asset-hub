"use client";
import { useState } from "react"; import { createClient } from "@/lib/supabase/client";
export function LoginForm() { const [error,setError]=useState(""); async function submit(f:FormData){setError("");const {error}=await createClient().auth.signInWithPassword({email:String(f.get("email")),password:String(f.get("password"))});if(error)setError(error.message);else location.href="/";} return <form action={submit} className="mt-8 space-y-4"><input required name="email" type="email" placeholder="邮箱" className="w-full rounded-xl border border-zinc-200 bg-white px-4 py-3 outline-none focus:border-orange-brand"/><input required name="password" type="password" placeholder="密码" className="w-full rounded-xl border border-zinc-200 bg-white px-4 py-3 outline-none focus:border-orange-brand"/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="w-full rounded-full bg-orange-brand py-3 font-medium text-white hover:bg-orange-deep">登录火麦-素材共享</button></form>; }
